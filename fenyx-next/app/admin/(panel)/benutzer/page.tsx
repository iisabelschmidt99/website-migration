export default function AdminBenutzer() {
  return (
    <div>
      <h1 className="text-2xl font-heading mb-2">Benutzer</h1>
      <p className="text-mist text-sm">
        Hier entsteht die Benutzerverwaltung (Tabelle <code>profiles</code>): Konten
        einsehen und Rollen vergeben (admin / editor / viewer). Nur für Admins.
      </p>
    </div>
  );
}
