export default function AdminReferenzen() {
  return (
    <div>
      <h1 className="text-2xl font-heading mb-2">Referenzen</h1>
      <p className="text-mist text-sm">
        Hier entsteht die Verwaltung der Referenzen (Tabelle <code>references</code>).
        Als Nächstes: Liste aus Supabase laden, Anlegen/Bearbeiten, Veröffentlichen.
      </p>
    </div>
  );
}
